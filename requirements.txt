fastapi>=0.110.0
uvicorn>=0.28.0
pydantic-settings>=2.2.0
sqlalchemy>=2.0.0          # SQL toolkit to talk to PostgreSQL
psycopg2-binary>=2.9.0     # PostgreSQL database adapter
langchain>=0.1.0           # Orchestration framework (or use llama-index)
chromadb>=0.4.0            # Vector Database (or use faiss-cpu / faiss-gpu)
prometheus-client>=0.20.0  # Exposes FastAPI metrics to Prometheus
mlflow>=2.11.0             # Experiment tracking
dvc>=3.43.0                # Data & model versioning
pytest>=8.0.0              # Testing framework
transformers>=4.30.0       # HuggingFace transformer models
torch>=2.0.0               # PyTorch (required by transformers)
datasets>=2.13.0           # HuggingFace datasets
scikit-learn>=1.3.0        # For train/test split
pandas>=2.0.0              # Data manipulation
vaderSentiment>=3.3.2      # Lexicon-based sentiment analysis (no LLM)
sentence-transformers>=2.2.0  # Lightweight embeddings (all-MiniLM-L6-v2)
xgboost>=2.0.0             # Gradient boosting for priority prediction